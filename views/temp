vector<vector<int>> overlap;
int n = interval.size();
overlap.push_back(interval[0]);
int i=0;
int j=1;
while(j<n){
    int m = overlap.size();
    if(overlap[m-1][1]<=interval[j][0]){
        overlap[m-1][1] = max(overlap[m-1][1], interval[j][1]);
    }
    else{
        overlap.push_back(interval[j]);
    }
    j++;
}
vector<vector<int>> ans;
for(auto merged:overlap){
    int a = merged[0];
    int b = merged[1];
    int deleteint = 0;
    if(a>=fa && a<=fb) a =fb+1;
    else if(b>=fa && b<=fb) b= fa-1;
    else if(a>=fa && b<=fb) deleteint =1;
    ans.push_back({a,b});
}
return ans;